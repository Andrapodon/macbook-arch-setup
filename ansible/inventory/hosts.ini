[macbook]
# Local execution directly on the MacBook:
localhost ansible_connection=local

# Remote execution by the maintainer over SSH or WireGuard:
# macbook-remote ansible_host=10.10.0.2 ansible_user=ramona ansible_ssh_private_key_file=~/.ssh/id_ed25519
